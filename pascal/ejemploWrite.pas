program ejemploWrite;
var lado, area : integer;
begin
	write(5);
	writeln(5,3,2);
	writeLn(4.53);
	lado := 10;
	area := lado * lado;
	write('si el lado de un cuadrado es ', lado);
	writeln(', el area es ', area);
end.
